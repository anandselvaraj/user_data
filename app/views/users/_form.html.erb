<%= form_with(model: user) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="login_cont">
<div class="form-group">
        <label for="name"><%= form.label :User_name%></label>
    <%= form.text_field :username, class: "form-control", placeholder: "Enter UserName" %>
  </div>

  <div class="form-group">
        <label for="name"><%= form.label :first_name %></label>
    <%= form.text_field :first_name, class: "form-control", placeholder: "Enter FirstName" %>
  </div>

    <div class="form-group">
        <label for="name"><%= form.label :last_name %></label>
    <%= form.text_field :last_name, class: "form-control", placeholder: "Enter LastName" %>
  </div>
    <div class="form-group">
        <label for="password"><%= form.label :password %></label>
    <%= form.password_field :password, class: "form-control", placeholder: "Enter Password" %>
  </div>

    <div class="form-group">
        <label for="number"><%= form.label :age %></label>
    <%= form.number_field :age, class: "form-control", placeholder: "Enter Age" %>
  </div>

    <div class="form-group">
        <label for="name"><%= form.label :address %></label>
    <%= form.text_area :address, class: "form-control", placeholder: "Enter Address" %>
  </div>

  <div class="actions">
    <%= form.submit "Save", class: "btn btn-primary" %>
  </div>
<% end %>
</div>